// Configuration de l'URL de base de l'API backend
export const API_CONFIG = {
  baseURL: 'http://localhost:5000/api', // URL du backend (port 5000 défini dans .env)
  timeout: 10000, // Timeout de 10 secondes
};